version: '3.8'

services:
  backend:
    build:
      context: ./hackable
    ports:
      - "8000:8000"
    volumes:
      - ./hackable/app:/app
    environment:
      - ENV=production

  frontend:
    build:
      context: ./hackable-vue
    ports:
      - "80:80"
    depends_on:
      - backend
